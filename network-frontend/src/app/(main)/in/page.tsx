import { Metadata } from "next";
    
export const metadata: Metadata = {
  title: "Profile",
  description: "Login to your account",
};

export default function LoginPage() {
  return <div className="mt-4 text-center text-sm">profile</div>;
}
